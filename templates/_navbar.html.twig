<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_sortie_index') }}">Sortir.com</a>

        <div class="navbar-nav">
            {# Liens accessibles à tous #}
            <a class="nav-link" href="{{ path('app_sortie_index') }}">Accueil</a>

            {% if app.user %}
                {# Liens pour utilisateurs connectés #}
                <a class="nav-link" href="{{ path('app_user_profile_edit') }}">Mon Profil</a>

                {% if is_granted('ROLE_ADMIN') %}
                    <a class="nav-link text-warning" href="{{ path('app_campus') }}">Gestion Campus</a>
                {% endif %}

                <a class="nav-link btn btn-outline-danger btn-sm ms-2" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                {# Liens pour visiteurs #}
                <a class="nav-link btn btn-outline-primary btn-sm ms-2" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>
